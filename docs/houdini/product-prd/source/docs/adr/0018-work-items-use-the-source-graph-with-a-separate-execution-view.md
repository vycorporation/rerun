# Work items use the source graph with a separate execution view

Houdini Clone keeps the node graph as the definition of what should happen and uses a separate execution panel to show what is queued, running, cached, canceled, superseded, failed, or complete. Work items do not create a second durable graph model; they are an operational view of demand-driven graph evaluation.
