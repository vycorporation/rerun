# Rerun spike starts as an external prototype

The Rerun-first validation spike should start as an external prototype using upstream Rerun through its existing Python, Rust, and viewer APIs. A maintained fork should happen only after benchmarks and integration tests show exactly which Houdini-like graph, layer, asset, or performance capabilities require invasive changes, because forking first creates maintenance burden before the missing surface area is understood.
