# Procedural assets encapsulate typed graphs

Houdini Clone procedural assets encapsulate one or more internal node graphs behind a typed asset interface. Users should be able to author assets and their public inputs, outputs, parameters, metadata, and documentation from the GUI so reusable workflows do not require plugin development or hand-written configuration.
