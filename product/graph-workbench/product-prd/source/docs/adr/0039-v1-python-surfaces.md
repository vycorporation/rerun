# V1 Python surfaces

Houdini Clone v1 exposes Python through a console for inspection and automation, Python operators for graph-backed library use, and Python expressions for parameter values. Arbitrary UI plugins, event callbacks, and untrusted web Python are deferred until the permission, environment, and security model is explicit.
