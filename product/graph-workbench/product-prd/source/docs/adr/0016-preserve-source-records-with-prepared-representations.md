# Preserve source records with prepared representations

Houdini Clone preserves source dataset references and logical records as the user's durable truth while allowing explicit or cached prepared representations for performance. Prepared representations must remain inspectable and reproducible so loading data does not silently mutate source geometry or attributes.
