# Node outputs can have multiple layer views

Houdini Clone allows a single compatible node output to appear as multiple layer views, each with its own name, visibility, order, and style binding. This supports comparison and alternate visual interpretations without duplicating expensive upstream graph evaluation.
