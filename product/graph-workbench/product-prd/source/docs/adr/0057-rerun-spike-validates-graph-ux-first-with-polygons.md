# Rerun spike validates graph UX first with polygons

The first Rerun external prototype should validate Houdini-like graph UX before point-cloud scale, using polygon-heavy and curve-heavy workflows as the initial scope. This sequence is acceptable because polygons and cubic Bezier curves are the primary geometry types, while point clouds remain an important later performance gate before committing to Rerun as the long-term foundation.
