# V1 layout centers on one primary viewport

Houdini Clone's project model should allow multiple viewports, but the v1 workspace layout centers on one primary viewport alongside the node graph, layer stack, parameter panel, operator browser, and execution panel. This leaves room for comparison and 2D/3D multi-view workflows later without making multi-viewport polish a first-milestone dependency.
