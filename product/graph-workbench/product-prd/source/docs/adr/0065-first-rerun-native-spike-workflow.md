# First Rerun-native spike workflow

The first Rerun-native graph spike should demonstrate a small polygon and cubic Bezier workflow inside or alongside Rerun's native Rust viewer framework: Source -> Filter -> Style -> Rerun Output. The spike should include layer visibility toggles, parameter editing, node info, and native-curve rendering feasibility so it proves the core Houdini-like loop before expanding into procedural assets, plugins, Python environments, or point-cloud performance gates.
