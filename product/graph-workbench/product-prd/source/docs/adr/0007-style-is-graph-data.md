# Style is graph data

Houdini Clone treats durable styling as typed graph data produced, connected, inspected, reused, and packaged through style operators. Viewport-only state remains limited to temporary viewing context so visual mappings stay reproducible and available to procedural assets.
