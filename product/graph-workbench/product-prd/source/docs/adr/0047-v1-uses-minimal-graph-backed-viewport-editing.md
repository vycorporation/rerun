# V1 uses minimal graph-backed viewport editing

Houdini Clone v1 supports minimal direct viewport editing for selection, inspection, and simple curve or polygon creation or correction where needed. Durable edits are represented as graph-backed manual edit nodes or source-edit operations, while full interactive drawing, CAD, GIS editing, and 3D modeling tools are deferred until the procedural exploration workflow is stable.
