# Python interface is a first-class desktop capability

Houdini Clone includes a Python interface so users can automate workflows and use Python libraries through trusted desktop/runtime pathways. Python-backed work should participate in the same typed graph contract as other operators, so Python operators remain inspectable, cacheable, reproducible, and compatible with procedural assets instead of becoming invisible ad hoc logic outside the graph.
