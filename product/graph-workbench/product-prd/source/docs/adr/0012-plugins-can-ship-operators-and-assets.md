# Plugins can ship operators and assets

Houdini Clone plugins can provide code-backed operators, graph-backed procedural assets, or both, but these concepts remain distinct. Operators add executable capabilities, while procedural assets package reusable graph workflows and should not require custom code unless they depend on plugin operators.
