# Custom shelf tools are deferred

Houdini Clone v1 provides built-in shelf tools for common graph-backed project commands. User-defined custom shelf tools are deferred until procedural assets, Python permissions, plugin capabilities, and the security model are stable enough to support them deliberately.
