# Plugin capabilities use execution tiers

Houdini Clone organizes plugin capabilities into graph assets, frontend plugin operators, native plugin operators, and remote plugin operators. V1 should support graph assets and a narrow trusted native plugin operator path, while arbitrary frontend plugins and remote execution are deferred until the security and permission model is explicit.
