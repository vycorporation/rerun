# Viewport supports 3D navigation from the start

Houdini Clone includes 3D camera navigation from the start because point clouds and 3D spatial datasets are first-class concerns. The first milestone should keep geometry behavior simple and avoid full 3D modeling tools, but bounds, selection, styling, prepared representations, and rendering should not assume a purely 2D viewport.
